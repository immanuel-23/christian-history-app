server:
  port: ${PORT:8080}
  shutdown: graceful
  tomcat:
    max-http-post-size: 10MB

spring:
  application:
    name: christian-history-backend
  datasource:
    url: jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres
    username: postgres.cglibjnheuolwjtrlzwv
    password: "Trycatvh@@1234"
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: ${JWT_SECRET:8fdfd4304b126301beaf8263bf6be6b9e223b207eecc80fc93bc678a6358c278} # Random default 256-bit key for local
  expirationMs: 86400000 # 1 day

# Authentication is now managed via Database (admin_users table)

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}
